#include <iostream>
#include <string>
using namespace std;

int main() {
    string a;
    string b;
    string c;
    cin>>a;
    cin>>b;
    int len=a.size();
    int len1=b.size();
    c=a+b;
    cout<<len<<" "<<len1<<endl;
    cout<<c<<endl;
    cout<<b[0]+a.substr(1,len-1)<<" "<<a[0]+b.substr(1,len1-1);
    /*for(int i=0;i<len;i++)
    {
        if(i==0)
        {
            cout<<b[i];
        }
        cout<<a[i+1];
    }
    cout<<" ";
    for(int i=0;i<len1;i++)
    {
        if(i==0)
        {
            cout<<a[i];
        }
        cout<<b[i+1];
    }*/

	
    return 0;
}

